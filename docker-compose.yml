services:
  music-automation:
    build: .
    container_name: lyra
    restart: unless-stopped
    network_mode: host
    dns:
      - 8.8.8.8
      - 1.1.1.1
    env_file:
      - .env
    volumes:
      - /mnt/Storage2/data/media/Music:/mnt/Storage2/data/media/Music
      - /DATA/Downloads:/DATA/Downloads
      # Mount the database directory so the SQLite state is persistent
      - ./data:/app/data
    environment:
      - DB_PATH=/app/data/music_automation.db
      - TZ=Asia/Jakarta # Change to your timezone if needed
